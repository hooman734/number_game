@using System.Reflection.Metadata


@if (Correct)
    {
        <audio autoplay><source src="Sounds/yes.mp3"/></audio>
    }

@if (Incorrect)
    {
        <audio autoplay><source src="Sounds/no.mp3"/></audio>
    }

@code {
    [Parameter]
    public bool Correct { get; set; }
    
    [Parameter]
    public bool Incorrect { get; set; }
}